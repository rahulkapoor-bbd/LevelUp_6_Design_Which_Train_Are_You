extends layout

block content
  div.heading.container
    h1=title
    p Please log in to take the quiz
    form(action="/login", method="POST")
      p
        section
          | Username: 
          input(type="text", name="username", value="")
        section
          | Password:
          input(type="password", name="password", value="")
      input(type="submit", value="Log In")

